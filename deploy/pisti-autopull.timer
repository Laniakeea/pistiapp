# /etc/systemd/system/pisti-autopull.timer
# Fires the auto-deploy check one minute after boot and every minute after.
[Unit]
Description=Run Pisti auto-deploy check every minute

[Timer]
OnBootSec=1min
OnUnitActiveSec=1min
AccuracySec=10s

[Install]
WantedBy=timers.target
